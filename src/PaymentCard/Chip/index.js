import React from 'react';
import PropTypes from 'prop-types';

import styles from './styles.scss';

const Chip = props => {
  return (
<span className='chip' />
  );
}

Chip.propTypes = {

};

export default Chip;
